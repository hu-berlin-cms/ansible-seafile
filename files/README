Place the following files here:

# ceph config
ceph.conf
client.seafile

# HTTPS certificates (certificate, chain and key)
# Place in ssl subdir!
## Certificate and Chain goes into chain subfolder:
ssl/chain/seafile-cert.pem
ssl/chain/seafile-chain.pem  # intermediate ca certificates
ssl/seafile-key.pem
## This layout is needed for Ansible assemble module and the various variants of SSL configs (nginx, Apache, haproxy)...

# license file
seafile-license.txt
